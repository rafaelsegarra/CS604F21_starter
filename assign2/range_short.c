#include <stdio.h>
int main() {

    short min_short, max_short;

    //TODO: Please find the range of short type, and print out the min and max of short in decimal
    //You are not allowed to hard-code two numbers in decimals
    //You are allowed to hard-code any binaries/hex
    //No loop or if
    printf("%hd ", min_short);
    printf("%hd\n", max_short);
    return 0;
}
